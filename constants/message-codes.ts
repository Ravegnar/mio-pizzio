export enum FlashMessageType {
   // eslint-disable-next-line no-unused-vars
   PIZZA_RELATION = "PIZZA_RELATION",
}

export const flashMessages = {
   [FlashMessageType.PIZZA_RELATION]: "The ingredient is attached to the following pizzas:",
};
